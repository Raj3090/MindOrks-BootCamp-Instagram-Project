<?xml version="1.0" encoding="utf-8"?>
<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
                name="viewModel"
                type="com.raj.sharephoto.ui.home.post.PostItemViewModel"/>
    </data>

    <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardUseCompatPadding="false"
            app:cardElevation="4dp">

        <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingTop="8dp"
                android:paddingBottom="16dp">

            <TextView
                    style="@style/AppTheme.Body1"
                    android:layout_width="0dp"
                    android:layout_height="38dp"
                    android:lines="1"
                    android:ellipsize="end"
                    tools:text="Erwin Smith"
                    android:text="@{viewModel.post.creator.name}"
                    android:id="@+id/tvName"
                    app:layout_constraintStart_toEndOf="@+id/imageView"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    android:layout_marginStart="8dp"
                    android:gravity="center_vertical"/>

            <ImageView
                    android:layout_width="38dp"
                    android:layout_height="38dp"
                    android:scaleType="centerCrop"
                    app:url="@{viewModel.post.creator.profilePicUrl}"
                    app:headers="@{viewModel.headers}"
                    app:circular="@{true}"
                    tools:ignore="ContentDescription"
                    tools:src="@drawable/selector_profile"
                    android:layout_marginStart="16dp"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    android:id="@+id/imageView"/>

            <ImageView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:scaleType="center"
                    app:url="@{viewModel.post.imageUrl}"
                    app:headers="@{viewModel.headers}"
                    app:placeholderWidth="@{viewModel.getPlaceholderWidth()}"
                    app:placeholderHeight="@{viewModel.getPlaceholderHeight()}"
                    tools:ignore="ContentDescription"
                    tools:src="@drawable/ic_photo"
                    app:layout_constraintStart_toStartOf="parent"
                    android:layout_marginTop="8dp"
                    app:layout_constraintTop_toBottomOf="@+id/tvName"
                    app:layout_constraintEnd_toEndOf="parent"
                    android:id="@+id/imageView2"/>

            <ImageView android:layout_width="32dp"
                       android:layout_height="32dp"
                       android:padding="6dp"
                       tools:src="@drawable/ic_heart_unselected"
                       app:layout_constraintTop_toBottomOf="@+id/imageView2"
                       app:layout_constraintStart_toStartOf="parent"
                       android:layout_marginStart="10dp"
                       android:layout_marginTop="5dp"
                       android:onClick="@{() -> viewModel.onLikeClick()}"
                       tools:ignore="ContentDescription"
                       android:src="@{safeUnbox(viewModel.isLiked) ? @drawable/ic_heart_selected : @drawable/ic_heart_unselected }"
                       android:id="@+id/imageView3"/>

            <TextView
                    style="@style/AppTheme.Body2"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="@string/post_like_label"
                    app:layout_constraintStart_toEndOf="@+id/imageView3"
                    android:layout_marginTop="10dp"
                    android:layout_marginStart="2dp"
                    android:text="@{@string/post_like_label(viewModel.likesCount)}"
                    app:layout_constraintTop_toBottomOf="@+id/imageView2"
                    android:id="@+id/textView"/>

            <TextView
                    style="@style/AppTheme.Body2"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="4 Hrs ago"
                    android:textColor="@color/grey"
                    android:layout_marginTop="10dp"
                    android:text="@{viewModel.getPostTime()}"
                    app:layout_constraintTop_toBottomOf="@+id/imageView2"
                    app:layout_constraintEnd_toEndOf="parent"
                    android:layout_marginEnd="16dp"/>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>